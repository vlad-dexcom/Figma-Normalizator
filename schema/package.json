{
  "name": "@figma-normalizator/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Versioned IR JSON Schema, generated TS types, and fixtures for the Figma Normalizator IR",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:types": "node scripts/generate-types.mjs"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "json-schema-to-typescript": "^15.0.4"
  }
}
